# Deprecated: use /claudiac/requirements.txt at repo root for cloud deploys.
# Kept for backwards compatibility: pip install -r server/requirements.txt
flask>=3.0.0
flask-cors>=4.0.0
gunicorn>=22.0.0
numpy>=1.26.0
scipy>=1.11.0
